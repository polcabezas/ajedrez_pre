# Requirements for the Chess project.
# This file lists the external Python libraries needed to run the application.

# Core libraries
pygame
python-dotenv

# Chess logic engine
python-chess

# Testing
pytest
mock # If using Python < 3.3, otherwise unittest.mock is built-in
pytest-cov # For coverage reports

# Optional: Type checking
mypy
